<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0, maximum-scale=1.0"
  />
  <title>
    <%= title ? `${title} | ${brand.name}` : `${brand.name} | ${brand.tagline}` %>
  </title>
  <meta
    name="description"
    content="<%= metaDescription || 'Classic car rentals for weddings, productions, and premium events across Malaysia with full concierge support.' %>"
  />
  <meta property="og:title" content="<%= title || brand.name %>" />
  <meta
    property="og:description"
    content="<%= metaDescription || 'Malaysia\'s trusted classic car experience for weddings, video shoots, and VIP moments.' %>"
  />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="<%= brand.name %>" />
  <meta property="og:url" content="<%= metaUrl || '' %>" />
  <meta property="og:image" content="<%= metaImage || '/images/og-default.jpg' %>" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Manrope:wght@400;500;600;700&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="icon" href="/images/favicon.ico" />
</head>
